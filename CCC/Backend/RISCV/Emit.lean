import CCC.IR.IR

namespace CCC.Backend.RISCV.Emit

open CCC.IR

-- Assembly emission: target instructions → assembly text
def emitAsm : String := panic! "not implemented: RISCV/emit"

end CCC.Backend.RISCV.Emit
